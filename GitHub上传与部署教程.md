# GitHub 上传与部署完整教程

> 本教程将指导你如何将图片字幕生成器项目上传到 GitHub，并部署到多个平台。

---

## 📋 目录

1. [准备工作](#准备工作)
2. [上传到 GitHub](#上传到-github)
3. [部署选项](#部署选项)
   - [方案一：GitHub Pages（推荐）](#方案一github-pages推荐)
   - [方案二：Vercel](#方案二vercel)
   - [方案三：Netlify](#方案三netlify)
   - [方案四：Cloudflare Pages](#方案四cloudflare-pages)

---

## 准备工作

### 1. 检查项目文件

确保你的项目包含以下核心文件：
- ✅ `index.html` - 主页面
- ✅ `style.css` - 样式文件
- ✅ `app.js` - 主应用逻辑
- ✅ `modules.js` - 模块文件
- ✅ `README.md` - 项目说明
- ✅ `.gitignore` - Git 忽略文件（已创建）

### 2. 安装 Git（如果未安装）

**macOS:**
```bash
# 检查是否已安装
git --version

# 如果未安装，使用 Homebrew 安装
brew install git
```

**Windows:**
- 下载并安装 [Git for Windows](https://git-scm.com/download/win)

**Linux:**
```bash
sudo apt-get install git  # Ubuntu/Debian
sudo yum install git      # CentOS/RHEL
```

### 3. 配置 Git（首次使用）

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

---

## 上传到 GitHub

### 步骤 1：在 GitHub 上创建仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角的 **"+"** → **"New repository"**
3. 填写仓库信息：
   - **Repository name**: `image-subtitle-generator`（或你喜欢的名字）
   - **Description**: 图片字幕生成器 - 为图片添加多行字幕的工具
   - **Visibility**: 选择 **Public**（公开）或 **Private**（私有）
   - ⚠️ **不要**勾选 "Initialize this repository with a README"（因为本地已有文件）
4. 点击 **"Create repository"**

### 步骤 2：初始化本地 Git 仓库

在项目目录下打开终端，执行以下命令：

```bash
# 进入项目目录
cd "/Users/andy/Projects/superhuang/Lesson 1"

# 初始化 Git 仓库
git init

# 添加所有文件到暂存区
git add .

# 提交文件
git commit -m "初始提交：图片字幕生成器项目"
```

### 步骤 3：连接远程仓库并推送

```bash
# 添加远程仓库（将 YOUR_USERNAME 替换为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/image-subtitle-generator.git

# 或者使用 SSH（如果你配置了 SSH 密钥）
# git remote add origin git@github.com:YOUR_USERNAME/image-subtitle-generator.git

# 推送代码到 GitHub
git branch -M main
git push -u origin main
```

**如果遇到认证问题：**

GitHub 已不再支持密码认证，需要使用 Personal Access Token：

1. 访问 [GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)](https://github.com/settings/tokens)
2. 点击 **"Generate new token (classic)"**
3. 设置权限：勾选 `repo`（完整仓库访问权限）
4. 生成后复制 token
5. 推送时，用户名输入你的 GitHub 用户名，密码输入刚才复制的 token

**或者使用 SSH 密钥（推荐）：**

```bash
# 生成 SSH 密钥
ssh-keygen -t ed25519 -C "your_email@example.com"

# 复制公钥内容
cat ~/.ssh/id_ed25519.pub

# 将公钥添加到 GitHub: Settings → SSH and GPG keys → New SSH key
```

### 步骤 4：验证上传成功

1. 刷新你的 GitHub 仓库页面
2. 应该能看到所有项目文件
3. 点击 `index.html` 可以查看代码

---

## 部署选项

现在你的代码已经在 GitHub 上了，接下来可以选择一个平台进行部署，让全世界都能访问你的应用。

---

## 方案一：GitHub Pages（推荐）

**优点：** 免费、简单、与 GitHub 集成、自动部署  
**缺点：** 仅支持静态网站、构建速度较慢

### 部署步骤

#### 方法 A：通过 GitHub 网页界面

1. 进入你的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 在左侧菜单找到 **Pages**
4. 在 **Source** 部分：
   - 选择 **Deploy from a branch**
   - Branch 选择 `main`
   - Folder 选择 `/ (root)`
5. 点击 **Save**
6. 等待几分钟，GitHub 会显示你的网站地址：
   - 格式：`https://YOUR_USERNAME.github.io/image-subtitle-generator/`

#### 方法 B：使用 gh-pages 分支（可选）

如果你希望将网站文件放在单独的分支：

```bash
# 创建并切换到 gh-pages 分支
git checkout -b gh-pages

# 推送 gh-pages 分支
git push origin gh-pages

# 切换回 main 分支
git checkout main
```

然后在 Settings → Pages 中选择 `gh-pages` 分支。

### 访问你的网站

部署完成后，访问：
```
https://YOUR_USERNAME.github.io/image-subtitle-generator/
```

### 更新网站

每次你推送代码到 `main` 分支后，GitHub Pages 会自动重新部署（通常需要 1-2 分钟）。

---

## 方案二：Vercel

**优点：** 部署速度快、自动 HTTPS、全球 CDN、支持自定义域名  
**缺点：** 需要注册账号

### 部署步骤

1. **访问 [Vercel](https://vercel.com)**
   - 点击 **"Sign Up"** 注册（可以使用 GitHub 账号登录）

2. **导入项目**
   - 点击 **"Add New..."** → **"Project"**
   - 选择 **"Import Git Repository"**
   - 选择你的 GitHub 仓库
   - 点击 **"Import"**

3. **配置项目**
   - **Framework Preset**: 选择 **"Other"** 或 **"Vite"**（虽然你用的是纯 HTML，但选择 Other 即可）
   - **Root Directory**: `./`（默认）
   - **Build Command**: 留空（纯静态项目不需要构建）
   - **Output Directory**: `./`（默认）

4. **部署**
   - 点击 **"Deploy"**
   - 等待部署完成（通常 30 秒内）

5. **访问网站**
   - 部署完成后，Vercel 会提供一个 URL，格式如：
     ```
     https://image-subtitle-generator-xxx.vercel.app
     ```

### 自动部署

- 每次你推送代码到 GitHub，Vercel 会自动重新部署
- 你可以在 Vercel 控制台查看部署历史

### 自定义域名

在 Vercel 项目设置中可以添加自定义域名（需要域名所有权验证）。

---

## 方案三：Netlify

**优点：** 免费、简单、支持表单处理、持续部署  
**缺点：** 需要注册账号

### 部署步骤

1. **访问 [Netlify](https://www.netlify.com)**
   - 点击 **"Sign up"** 注册（可以使用 GitHub 账号登录）

2. **导入项目**
   - 点击 **"Add new site"** → **"Import an existing project"**
   - 选择 **"Deploy with GitHub"**
   - 授权 Netlify 访问你的 GitHub
   - 选择你的仓库

3. **配置构建设置**
   - **Build command**: 留空
   - **Publish directory**: `./`（或留空）

4. **部署**
   - 点击 **"Deploy site"**
   - 等待部署完成

5. **访问网站**
   - Netlify 会提供一个随机 URL，格式如：
     ```
     https://random-name-123456.netlify.app
     ```
   - 你可以在站点设置中修改为自定义名称

### 自定义域名

在站点设置 → Domain management 中可以添加自定义域名。

---

## 方案四：Cloudflare Pages

**优点：** 免费、全球 CDN、无限带宽、快速部署  
**缺点：** 需要注册账号

### 部署步骤

1. **访问 [Cloudflare Pages](https://pages.cloudflare.com)**
   - 注册或登录 Cloudflare 账号

2. **创建项目**
   - 点击 **"Create a project"**
   - 选择 **"Connect to Git"**
   - 选择 GitHub 并授权
   - 选择你的仓库

3. **配置构建**
   - **Framework preset**: 选择 **"None"** 或 **"Static"**
   - **Build command**: 留空
   - **Build output directory**: `./`

4. **部署**
   - 点击 **"Save and Deploy"**
   - 等待部署完成

5. **访问网站**
   - Cloudflare 会提供一个 URL，格式如：
     ```
     https://image-subtitle-generator.pages.dev
     ```

---

## 🎯 推荐方案对比

| 平台 | 部署速度 | 易用性 | 自定义域名 | 推荐指数 |
|------|---------|--------|-----------|---------|
| **GitHub Pages** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ 免费 | ⭐⭐⭐⭐⭐ |
| **Vercel** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ 免费 | ⭐⭐⭐⭐⭐ |
| **Netlify** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ 免费 | ⭐⭐⭐⭐ |
| **Cloudflare Pages** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ 免费 | ⭐⭐⭐⭐ |

### 我的建议

- **如果你是 GitHub 用户**：优先选择 **GitHub Pages**，最简单直接
- **如果你想要最快的部署速度**：选择 **Vercel**
- **如果你需要更多功能**（如表单处理）：选择 **Netlify**

---

## 📝 后续维护

### 更新网站内容

1. 在本地修改代码
2. 提交更改：
   ```bash
   git add .
   git commit -m "更新：描述你的更改"
   git push origin main
   ```
3. 平台会自动重新部署（通常 1-2 分钟）

### 查看部署状态

- **GitHub Pages**: Settings → Pages → 查看部署历史
- **Vercel**: Dashboard → 查看部署列表
- **Netlify**: Site overview → Deploys
- **Cloudflare Pages**: Dashboard → 查看部署历史

---

## ❓ 常见问题

### Q1: 部署后网站无法访问？

**检查清单：**
- ✅ 确认所有文件都已推送到 GitHub
- ✅ 检查 `index.html` 中的资源路径是否正确（使用相对路径）
- ✅ 查看浏览器控制台是否有错误
- ✅ 等待几分钟（首次部署可能需要时间）

### Q2: 如何更新网站？

只需推送新代码到 GitHub，平台会自动重新部署。

### Q3: 可以同时使用多个平台吗？

可以！你可以在多个平台部署同一个项目，每个平台都会提供独立的 URL。

### Q4: 如何添加自定义域名？

1. 在平台设置中找到 "Custom Domain" 或 "Domain" 选项
2. 添加你的域名
3. 按照平台提示配置 DNS 记录
4. 等待 DNS 生效（通常几分钟到几小时）

### Q5: 部署后图片路径错误？

确保在 HTML/CSS/JS 中使用**相对路径**，例如：
- ✅ `./style.css`
- ✅ `images/logo.png`
- ❌ `/style.css`（绝对路径可能在某些平台不工作）

---

## 🎉 完成！

恭喜！你的图片字幕生成器现在已经：
- ✅ 上传到 GitHub
- ✅ 部署到互联网
- ✅ 可以被全世界访问

**下一步建议：**
- 分享你的网站链接给朋友
- 在 README.md 中添加部署链接
- 考虑添加更多功能（如保存设置、导出配置等）

---

## 📚 参考资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com)
- [Cloudflare Pages 文档](https://developers.cloudflare.com/pages)

---

**祝你部署顺利！** 🚀
